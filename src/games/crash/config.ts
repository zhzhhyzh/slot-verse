import type { BaseGameConfig } from '../../utils/gameTypes';
export const crashConfig: BaseGameConfig = {
  id: 'crash', name: 'Crash',
  description: 'Watch the multiplier rise — cash out before it crashes! How high can you go?',
  type: 'crash', category: 'specialty', thumbnail: '🚀', themeColor: '#f39c12',
  minBet: 10, maxBet: 20000,
};
